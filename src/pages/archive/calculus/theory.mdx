---
layout: ../../../layouts/TheoryLayout.astro
title: Математический анализ
icon: chart-up
subheader: кафедра высшей математики-3
---

#### 1. Определение числового ряда, его сходимости. Примеры сходящихся и расходящихся рядов. Необходимый признак сходимости числового ряда. Признаки сравнения рядов с положительными членами.

**Числовой ряд** — бесконечная сумма членов числовой последовательности $\{a_n\}$, то есть:

$a_1 + a_2 + \dots + a_n + \dots = \sum_{n=1}^{\infty} a_n$

При этом $a_n$ называется *общим членом* ряда.

*Частичными суммами* ряда называются суммы вида:

$S_1 = a_1; S_2 = a_1 + a_2; \dots; S_n = a_1 + a_2 + \dots + a_n$ и т. д.

Эти суммы сами по себе создают числовую последовательность: $S_1, S_2, \dots, S_n, \dots$

Числовой ряд называется **сходящимся** если существует конечный предел последовательности его частичных сумм:

$\exists \displaystyle{\lim_{n \to \infty}} S_n = S$

Тогда число $S$ называется *суммой ряда.* Если предела не существует или он равен бесконечности, ряд называют *расходящимся.*

##### Примеры рядов
| Сходящиеся                             | Расходящиеся            |
| -------------------------------------- | ----------------------- |
| $\sum_{n=1}^{\infty} \frac{1}{n(n+1)}$ | $\sum_{n=1}^{\infty} 1$ |

##### Необходимый признак сходимости ряда
Если числовой ряд $\sum_{n=1}^{\infty} a_n$ сходится, то его **общий член стремится к нулю,** то есть:

$\displaystyle{\lim_{n \to \infty}} a_n = 0$

Если это условие не выполняется, ряд заведомо *расходится.*

##### Признаки сравнения рядов с положительными членами
Положительным ряд называется, если все его члены *неотрицательны.*

Пусть даны два положительных ряда $\sum_{n=1}^{\infty} a_n$ и $\sum_{n=1}^{\infty} b_n$.

###### Первый признак сравнения
Если для всех номеров $n$ *(или для всех номеров, бóльших некоторого номера $𝑁$)* выполнено неравенство $a_n \le b_n$, то:
из сходимости ряда $\sum_{n=1}^{\infty} b_n$ следует сходимость ряда  $\sum_{n=1}^{\infty} a_n$, а из расходимости ряда $\sum_{n=1}^{\infty} a_n$ следует расходимость ряда $\sum_{n=1}^{\infty} b_n$.

**Коротко:** из сходимости большего следует сходимость меньшего и наоборот.

###### Второй (предельный) признак сравнения
Если существует конечный, отличный от нуля $\displaystyle{\lim_{n \to \infty} \frac{a_n}{b_n}}$ , то ряды $\sum_{n=1}^{\infty} a_n$ и  $\sum_{n=1}^{\infty} b_n$ сходятся или расходятся одновременно.

**Коротко:** находим предел отношения при бесконечности общих членов. Если он конечный, то ряды *ведут себя одинаково.*

#### 2. Признак Даламбера. Радикальный и интегральный признаки Коши сходимости рядов с положительными членами.

При применении признаков сравнения необходимо подбирать известные сходящиеся или расходящиеся ряды. Признак Даламбера позволяет решить вопрос о сходимости ряда, проделав некоторые действия с самим рядом.

##### Признак Даламбера
Пусть дан положительный ряд $\sum_{n=1}^{\infty} a_n$ и существует конечный $\displaystyle{\lim_{n \to \infty}} \frac{a_{n+1}}{a_n} = D$. Тогда:

Если $D < 1$, то ряд **сходится;**
Если $D > 1$, то ряд **расходится;**
Если $D = 1$, то признак Даламбера результата не даёт.

**Примечание:** признак Даламбера удобно применять, когда общий член ряда содержит $n!$ или $a^n$.

##### Радикальный признак Коши
Пусть дан положительный ряд $\sum_{n=1}^{\infty} a_n$ и существует $\displaystyle{\lim_{n \to \infty}} \sqrt[n]{a_n} = K$. Тогда:

Если $K < 1$, то ряд **сходится;**
Если $K > 1$, то ряд **расходится;**
Если $K = 1$, то радикальный признак Коши ответа не даёт.

##### Интегральный признак Коши
Пусть дан положительный ряд $\sum_{n=1}^{\infty} a_n$, общий член которого можно рассматривать как функцию от n: $a_n = f(n)$. Предположим, что функция $f(x)$ определена, положительна, непрерывна и монотонно убывает при $x \ge 1$. Тогда этот ряд **сходится**, если сходится $\displaystyle{\int_{1}^{+\infty}} f(x) dx$, и **расходится,** если этот интеграл расходится.

#### 3. Определение знакочередующегося числового ряда. Определение абсолютной и условной сходимости. Признак Лейбница сходимости знакочередующегося числового ряда.

Числовой ряд называется *знакопеременным*, если среди его членов есть как положительные, так и отрицательные числа.

Если отрицательных членов конечное число, то, отбросив их, получим положительный ряд. Если положительных членов конечное число, то, отбросив их, получим отрицательный ряд, который можно исследовать с помощью теорем о сходимости положительных рядов, изменив знаки всех членов ряда.

Существенно новым является тот случай, когда среди членов ряда бесконечное число положительных и бесконечное число отрицательных чисел.

Ряды, представленные в виде $\sum_{i=1}^{\infty} (-1)^{n+1}\cdot a_n$ или $\sum_{i=1}^{\infty} (-1)^{n} \cdot a_n$, где $a_n > 0$, называют **знакочередующимися.**

##### Признак Лейбница сходимости знакочередующегося ряда
Если члены знакочередующегося ряда монотонно убывают по модулю: $a_n > a_{n+1}$ и стремятся к нулю:  $\displaystyle{\lim_{n \to \infty}} a_n = 0$, то ряд **сходится.**

Знакочередующийся ряд, удовлетворяющий условиям теоремы Лейбница, называется *рядом Лейбница.*

##### Абсолютная и условная сходимость
Пусть дан произвольный знакопеременный ряд $\sum_{i=1}^{\infty} a_n$, а ряд $\sum_{i=1}^{\infty} \lvert a_n \rvert$ составлен из модулей его членов.

Если сходится ряд из модулей членов данного ряда, то сходится и сам знакопеременный ряд — в таком случае он называется *абсолютно сходящимся*.

Если знакопеременный ряд сходится, а ряд, составленный из модулей его членов, расходится, то такой ряд называется *условно сходящимся.*

#### 4. Свойства абсолютно сходящихся рядов. Свойства условно сходящихся рядов. Примеры.

Если ряд сходится абсолютно, то ряд, полученный произвольной перестановкой его членов, также сходится и имеет ту же сумму. Другими словами, абсолютно сходящийся ряд обладает переместительным свойством так же, как и конечная сумма.

Если ряд сходится условно, то надлежащей перестановкой его членов можно изменить сумму ряда на любое заданное число, а также сделать ряд расходящимся.

**Примеры абсолютно сходящихся рядов:**

$\sum_{n=1}^{\infty} (-1)^{n+1} (\frac{3n+1}{5n+3})^n$

**Примеры условно сходящихся рядов:**

$\sum_{n=1}^{\infty} \frac{(-1)^n}{\sqrt[3]{n}}$ 

#### 5. Определение функционального ряда, его области сходимости. Равномерная сходимость функционального ряда. Теорема Вейерштрасса.

##### Функциональные ряды
Пусть дана последовательность функций: $u_1(x), u_2(x), \dots, u_n(x), \dots$ определённых на некотором множестве $X$.

Выражение вида:
$$
u_1(x) + u_2(x) + \dots + u_n(x) + \dots = \sum_{n=1}^{\infty} u_n(x)
$$
называется **функциональным рядом,** а множество $X$ — областью определения этого ряда.

Если на место $x$ подставить значение из $X$, то функциональный ряд превратится в обычный числовой, причем при разных значениях он может как расходиться, так и сходиться.

Множество значений переменной $x$, при которых функциональный ряд сходится, называется **областью сходимости** этого ряда.

##### Равномерная сходимость
Функциональный ряд $\sum_{n=1}^{\infty} u_n(x)$ называется равномерно сходящимся на множестве $X$, если этот ряд сходится при всех $x$, принадлежащих множеству $X$, и для любого $\varepsilon > 0$ найдется такой номер $N$, что для всех номеров $n > N$ и для любого $x \in X$ будет выполнено неравенство: $\lvert S_n(x) - S(x) \rvert < \varepsilon$.

**Короче:** ряд сходится равномерно, если разница между суммой и частичной суммой для всех $x$ из области определения сколь угодно мала, начиная с некоторого номера $n$.

Приняты такие обозначения:

$\sum_{n=1}^{\infty} u_n(x) = S(x)$ — ряд сходится.

$\sum_{n=1}^{\infty} u_n(x) \rightrightarrows S(x)$ — ряд сходится *равномерно.*

##### Теорема Вейерштрасса (достаточное условие равномерной сходимости)
Если члены функционального ряда $\sum_{n=1}^{\infty} u_n(x)$ при всех $x \in X$, удовлетворяют неравенству:
$$
\lvert u_n(x) \rvert \le a_n, n = 1, 2, 3, \dots
$$
где $a_n$ — члены некоторого сходящегося числового ряда $\sum_{n=1}^{\infty} a_n$,
то функциональный ряд сходится абсолютно и равномерно на множестве $X$.

Числовой ряд $\sum_{n=1}^{\infty} a_n$, удовлетворяющий условиям теоремы Вейерштрасса, называется **мажорирующим** числовым рядом для функционального ряда $\sum_{n=1}^{\infty} u_n(x)$ или **числовой мажорантой.**

#### 6. Теорема о непрерывности суммы функционального ряда. Теорема о почленном интегрировании функционального ряда. Теорема о почленном дифференцировании функционального ряда.

##### Теорема о непрерывности суммы функционального ряда
Пусть функции $u_n(x)$ определены и непрерывны на отрезке $[a, b]$, а ряд $\sum_{n=1}^{\infty} u_n(x)$ сходится равномерно на этом отрезке.

Тогда сумма ряда $S(x)$ непрерывна на этом отрезке.

##### Теорема о почленном интегрировании функционального ряда
Если функции $u_n(x)$ непрерывны на отрезке $[a,b]$, а ряд $\sum_{n=1}^{\infty} u_n(x)$ сходится равномерно на этом отрезке, то интеграл от суммы ряда $S(x)$ на отрезке $[a, b]$ представляется в виде суммы интегралов от членов этого ряда:
$$
\int_{a}^{b} S(x)dx = \int_{a}^{b} \sum_{n=1}^{\infty} u_n(x) dx = \sum_{n=1}^{\infty} \int_{a}^{b} u_n(x)dx
$$

##### Теорема о почленном дифференцировании функционального ряда
Пусть функции $u_n(x)$ определены и непрерывны на отрезке $[a,b]$ и имеют на этом отрезке непрерывные производные $u_n'(x)$. Ряд $\sum_{n=1}^{\infty} u_n(x)$ сходится, а ряд $\sum_{n=1}^{\infty} u_n'(x)$ сходится равномерно на отрезке $[a,b]$. Тогда сумма $S(x)$ ряда $\sum_{n=1}^{\infty} u_n(x)$ дифференцируема на отрезке $[a,b]$, причем производная суммы равна сумме ряда из производных:
$$
S'(x) = \biggl( \sum_{n=1}^{\infty} u_n(x) \biggr)' = \sum_{n=1}^{\infty} u_n'(x)
$$

#### 7. Определение степенного ряда. Теорема Абеля. Радиус сходимости степенного ряда. Равномерная сходимость степенного ряда. Непрерывность суммы степенного ряда. Теоремы о почленном интегрировании и дифференцировании степенных рядов.

**Степенным рядом** называется функциональный ряд вида: $\sum_{n=0}^{\infty} a_n(x - x_0)^n = a_0 + a_1x + a_2x^2 + \dots$

Рассматриваются также степенные ряды более общего вида: $\sum_{n=0}^{\infty} (x-x_0)^n = a_0 + a_1(x - x_0) + a_2(x-x_0)^2 + \dots$

Такие ряды с помощью замены переменной сводятся к виду $\sum_{n=0}^{\infty}a_nx^n$. Этот вид степенных рядов и будем изучать.

Любой степенной ряд сходится при $x = 0$.
##### Теорема Абеля
Если степенной ряд $\sum_{n=0}^{\infty} a_nx^n$ сходится в некоторой точке $x_0 \ne 0$, то он абсолютно сходится в любой точке $x$, такой что $\lvert x \rvert < \lvert x_0 \rvert$.

##### Интервал и радиус сходимости степенного ряда
Рассмотрим ряд $\sum_{n=0}^{\infty} n! x^n$. Этот ряд сходится только в одной точке — $x_0 = 0$.

Предположим, что для степенного ряда существуют отличные от нуля значения, при которых он сходится. Если множество этих значений не ограничено, то такой ряд по теореме Абеля сходится всюду, причём абсолютно.

Пусть множество этих значений ограничено, причём $R$ — точная верхняя грань этого множества.
Тогда степенной ряд сходится абсолютно в пределах интервала $(-R, R)$ и расходится вне этого интервала. На концах интервала ряд может как сходиться, так и расходиться.

Такой интервал $(-R, R)$ называется **интервалом сходимости,** а число $R$ — **радиусом сходимости.** Если степенной ряд сходится на всей числовой оси, его радиус сходимости $R = \infty$, а если ряд сходится только в точке $x = 0$, то его $R = 0$.

Интервал сходимости может быть найден с помощью признаков Даламбера и Коши, а вот чтобы установить сходимость на границах интервала понадобятся другие теоремы.

##### Равномерная сходимость степенного ряда
Степенной ряд $\sum_{n=0}^{\infty} a_nx^n$ сходится равномерно на любом отрезке, принадлежащем интервалу сходимости.

Из этого следует, что сумма степенного ряда всегда непрерывна на всём интервале сходимости.

##### Почленное интегрирование и дифференцирование степенных рядов
Интеграл от суммы степенного ряда $S(x)$ на любом отрезке, принадлежащем интервалу сходимости, равен сумме ряда, полученного из данного степенного ряда путем почленного интегрирования на том же отрезке:
$$
\int_{a}^{b} S(x)dx = \int_{a}^{b} \big( \sum_{n=0}^{\infty} a_nx^n \big) dx = \sum_{n=0}^{\infty} \int_{a}^{b} a_nx^n dx
$$

Если в качестве отрезка интегрирования взять отрезок $[0, 𝑥]$, где $x \in (-R, R)$, то равенство приобретает вид:
$$
\int_{a}^{b} S(x)dx = \sum_{n=0}^{\infty} \frac{a_n}{n+1}x^{n+1} = \sum_{n=1}^{\infty} \frac{a_{n-1}}{n} x^n
$$

То есть в результате почленного интегрирования суммы ряда на отрезке $[0, x]$ получается тоже степенной ряд.

При почленном дифференцировании степенного ряда получим также степенной ряд:
$$
\Bigg( \sum_{n=0}^{\infty} a_nx^n \Bigg)' = \sum_{n=0}^{\infty} (a_nx^n)' = \sum_{n=1}^{\infty} a_n \cdot nx^{n-1}
$$

Причём он будет иметь тот же радиус сходимости.

Это означает, что сумма степенного ряда дифференцируема на интервале сходимости, и производная суммы равна сумме ряда из производных.
